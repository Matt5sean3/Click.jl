abstract Bounds

"""
```julia
check_bounds(bounds::Bounds, x::Number, y::Number)
```

Returns true when the cursor is within the bounds and false otherwise
"""
check_bounds(bounds::Bounds, x::Number, y::Number) = 
  error("Bounds object is not correctly implemented")
